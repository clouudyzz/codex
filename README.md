# Codex Sample Application

This repository contains a simple authentication GUI built with Python's Tkinter.

## Requirements
- Python 3.x

## Running the App
Execute:

```bash
python3 app.py
```

The application allows users to register with a username, email and password.
Credentials are stored in a local `users.json` file.
Registered users can then log in with their credentials.
